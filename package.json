{
  "name": "moontui",
  "private": true,
  "version": "0.1.0",
  "description": "MoonTUI - A TUI library with Rust core and TypeScript bindings",
  "license": "MIT",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "cargo build && bun run --cwd packages/core build -- --lib",
    "build:rust": "cargo build -p moontui-core --release",
    "build:ts": "bun run --cwd packages/core build -- --lib",
    "build:native": "bun run --cwd packages/core build -- --native",
    "build:codegen": "bun scripts/generate-ffi.ts",
    "test": "cargo test && bun test --cwd packages/core",
    "test:rust": "cargo test",
    "test:ts": "bun test --cwd packages/core",
    "fmt": "cargo fmt --all && ultracite fix",
    "fmt:check": "cargo fmt --all -- --check && ultracite check",
    "fmt:rust": "cargo fmt --all",
    "fmt:rust:check": "cargo fmt --all -- --check",
    "fmt:ts": "ultracite fix",
    "fmt:ts:check": "ultracite check",
    "lint": "cargo clippy --workspace --all-targets -- --deny warnings && ultracite check",
    "lint:rust": "cargo clippy --workspace --all-targets -- --deny warnings",
    "lint:ts": "ultracite check",
    "check": "cargo fmt --all -- --check && ultracite check && cargo clippy --workspace --all-targets -- --deny warnings && ultracite check",
    "typecheck": "bun run --cwd packages/core typecheck",
    "clean": "rm -rf target/ packages/core/dist/ packages/core/native/",
    "prepare-release": "bun scripts/prepare-release.ts",
    "pre-publish": "bun scripts/pre-publish.ts",
    "publish": "bun run --cwd packages/core publish"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.15",
    "@moontui/config": "workspace:*",
    "typescript": "^6.0.3",
    "ultracite": "7.8.0",
    "@types/bun": "1.3.14"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
